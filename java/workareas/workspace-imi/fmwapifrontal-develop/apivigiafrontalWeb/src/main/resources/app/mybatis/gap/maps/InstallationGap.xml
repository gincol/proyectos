<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="es.bcn.imi.framework.vigia.frontal.gap.inventary.orm.dao.InstallationGapDao">
   
    <select id="getInstallationsGap" parameterType="java.util.Map" resultType="es.bcn.vigia.fmw.libcommons.integration.orm.model.gap.InstallationGap">
        SELECT 
	      	CODI AS code,
	      	DESCRIPCIO AS description,
			POSICIO_X AS coordinateX,
			POSICIO_Y AS coordinateY,
			SUPERFICIE AS surface,
			DATA_INICI AS startDate,
			DATA_FI AS endDate,
			SUBJECTE_AMORTITZACIO AS amortizationSubject
		FROM 
			VW_INSTALACIO
		WHERE 
			CODI = #{code}
		AND
			DATA_INICI <![CDATA[<=]]> SYSDATE AND (DATA_FI  <![CDATA[>=]]> SYSDATE OR DATA_FI IS NULL)
		<if test="checkStatusDelete != null">
			AND ESTAT_CODI !='B'
		</if>			
    </select>
    
</mapper>
	
	
