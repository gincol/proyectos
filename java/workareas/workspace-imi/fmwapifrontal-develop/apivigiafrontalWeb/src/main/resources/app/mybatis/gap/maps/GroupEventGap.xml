<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="es.bcn.imi.framework.vigia.frontal.gap.execution.orm.dao.GroupEventGapDao">
   
    <select id="getGroupEventsGap" parameterType="java.util.Map" resultType="es.bcn.vigia.fmw.libcommons.integration.orm.model.gap.GroupEventGap">
		SELECT 
	        CODI AS codeGroup, 
			DESCRIPCIO AS descriptionGroup,
			TIPUS_CODI AS codeType, 
			TIPUS_DESCRIPCIO AS descriptionType
		FROM 
			VW_DM_GRUP_ESDEVENIMENT
		WHERE 
			CODI = #{codeGroup}
		AND
			TIPUS_CODI = #{codeEventType}
	</select>
    
</mapper>
