<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="es.bcn.imi.framework.vigia.inventari.integration.orm.dao.gap.LabelGapDao">
   
    <select id="selectLabel" parameterType="java.util.Map" resultType="es.bcn.vigia.fmw.libcommons.integration.orm.model.gap.LabelGap">
    	SELECT
    		VALOR as value,
			CATEGORIA as category,
			TIPUS as type
		FROM 
			VW_ETIQUETA
		WHERE 
			1=1
			<if test="value != null">
				AND	VALOR = #{value}
			</if>
			<if test="category != null">
				AND	CATEGORIA = #{category}
			</if>
			<if test="type != null">
				AND	TIPUS = #{type}
			</if>
    </select>
</mapper>